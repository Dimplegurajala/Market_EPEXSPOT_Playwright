[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Ensure these flags are present to trigger the report generation
addopts = --headed --browser chromium --html=report.html --self-contained-html